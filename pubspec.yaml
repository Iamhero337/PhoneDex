name: phonedex
description: "PhoneDex - Transform your Android into a desktop experience with wireless-first reliability"
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  window_manager: ^0.3.8
  bitsdojo_window: ^0.1.6
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true